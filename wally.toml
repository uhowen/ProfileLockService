[package]
name = "uhowen/profilelockservice"
description = "Session-locked Roblox profile persistence with autosave, migrations, retries, and clean profile APIs."
version = "0.2.0"
license = "MIT"
authors = ["Owen"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["**"]
include = ["wally.toml", "default.project.json", "init.luau", "src", "examples", "README.md", "LICENSE"]

[dependencies]
