[package]
name = "uhowen/profilelockservice"
description = "Roblox profile data storage with session locks, autosave, migrations, and queued writes."
version = "0.2.1"
license = "MIT"
authors = ["Owen"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
exclude = ["**"]
include = ["wally.toml", "default.project.json", "init.luau", "src", "examples", "README.md", "LICENSE"]

[dependencies]
